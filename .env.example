# Rename to .env.local and set your Gemini API key
# This file is just an example and should NOT contain real secrets.

# Gemini API Key used by Vite to inject into client code
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Optional backend for Veo generation (client will POST to `${VITE_VEO_BACKEND_URL}/generate-veo`)
# Defaults to https://veo-backend.onrender.com if not set
VITE_VEO_BACKEND_URL=

# Autopilot default toggle (fallback only). The real state is persisted in KV.
# Accepts "true" or "false". If missing, defaults to ON (true).
AUTOPILOT_ENABLED=true

# Vercel KV (REST) configuration
# NOTE: Se usa REST KV; los valores son inyectados por Vercel Storage al conectar la base con el proyecto.
# Normalmente no necesitas definirlos manualmente si ya conectaste la base desde Storage → Connect Project.
# Si fuera necesario, puedes copiarlos desde la sección de REST API en la base y pegarlos aquí.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Redis connection (opcional)
# Nuestro código toma la primera disponible entre:
#   REDIS_URL || KV_REDIS_URL || VERCEL_REDIS_URL
# Si ya conectaste Redis desde Vercel Storage, KV_REDIS_URL suele estar presente automáticamente.
# No pegues secretos reales en este archivo; úsalos sólo en .env.local.
# Ejemplos:
# KV_REDIS_URL=redis://default:<password>@<host>:<port>
# REDIS_URL=rediss://default:<password>@<host>:<tls_port>
# VERCEL_REDIS_URL=redis://default:<password>@<host>:<port>
